<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="300dp"
    android:layout_height="200dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".SortListFragmeent">

    <ImageView
        android:id="@+id/closeDialog"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:src="@drawable/ic_close_black_24dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

   <Spinner
       android:id="@+id/sortLisField"
       android:entries="@array/sort_list_field"
       app:layout_constraintTop_toTopOf="parent"
       app:layout_constraintStart_toStartOf="parent"
       app:layout_constraintEnd_toEndOf="parent"
       android:layout_marginTop="40dp"
       android:layout_width="match_parent"
       android:layout_height="wrap_content" />


    <Spinner
        android:id="@+id/sortListype"
        android:layout_width="match_parent"
        android:entries="@array/sort_list_type"
        android:layout_marginBottom="40dp"
        app:layout_constraintBottom_toTopOf="@id/sortListButton"
        app:layout_constraintStart_toStartOf="@id/sortListButton"
        app:layout_constraintEnd_toEndOf="@id/sortListButton"
        android:layout_height="wrap_content" />


    <Button
        android:id="@+id/sortListButton"
        android:text="Sort"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</androidx.constraintlayout.widget.ConstraintLayout>